{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && yarn install --frozen-lockfile && cd apps/backend && npx prisma generate && npx prisma migrate deploy && yarn build"
  },
  "deploy": {
    "startCommand": "yarn start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
