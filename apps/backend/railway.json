{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install -g yarn && WORK_DIR=$PWD && cd ../.. && yarn install --frozen-lockfile && cd $WORK_DIR && npx prisma generate && npx prisma migrate deploy && yarn build"
  },
  "deploy": {
    "startCommand": "yarn start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
